{
  "name": "@agentops/ingest",
  "version": "0.1.0",
  "description": "AgentOps event ingestion API",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "eslint src/"
  },
  "dependencies": {
    "hono": "^4.12.12",
    "@hono/zod-validator": "^0.7.6",
    "zod": "^4.3.6",
    "@agentops/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260413.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.82.0"
  },
  "author": "Jose David Baena",
  "license": "MIT"
}
