/**
 * AgentOps SDK - Embedded Agent Analytics Module
 *
 * Embeddable analytics widgets with token-based access control,
 * configurable themes, and HTML/iframe rendering.
 *
 * @packageDocumentation
 */

export { EmbedSDKEngine } from "./engine.js";

export type {
  EmbedConfig,
  EmbedToken,
  EmbedScope,
  WidgetType,
  WidgetConfig,
  WidgetTheme,
  WidgetFilter,
  WidgetData,
  SessionTimelineData,
  CostBreakdownData,
  QualityScoreData,
  UsageChartData,
  ErrorFeedData,
  EmbedRenderOutput,
  EmbedMetrics,
} from "./types.js";
